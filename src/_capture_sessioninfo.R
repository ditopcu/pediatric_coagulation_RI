suppressPackageStartupMessages({
  library(tidyverse); library(readxl); library(refineR); library(gamlss)
  library(patchwork); library(ggrepel); library(cowplot); library(janitor)
  library(moments); library(openxlsx); library(officer); library(flextable)
})
dir.create("docs", showWarnings = FALSE)
sink("docs/sessionInfo.txt")
cat("# R sessionInfo snapshot -- Pediatric Coagulation RI project\n")
cat("# Generated by src/_capture_sessioninfo.R\n")
cat("# Re-run this script after upgrading any package to refresh.\n\n")
print(sessionInfo())
sink()
cat("[OK] docs/sessionInfo.txt written.\n")
